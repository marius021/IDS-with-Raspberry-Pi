{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Docker: Train (csv-preprocessing.py)",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: train",
      "python": {
        "pathMappings": [
          {"localRoot": "${workspaceFolder}", "remoteRoot": "/app"}
        ],
        "projectType": "general",
        "justMyCode": false
      }
    },
    {
      "name": "Docker: Infer (ids_inference.py)",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: infer",
      "python": {
        "pathMappings": [
          {"localRoot": "${workspaceFolder}", "remoteRoot": "/app"}
        ],
        "projectType": "general",
        "justMyCode": false
      }
    }
  ]
}
